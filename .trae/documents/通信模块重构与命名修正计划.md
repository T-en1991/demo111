我已完成对现有代码的深度分析，为您制定了详细的重构计划。

### 核心问题总结
1.  **逻辑混乱**：协议解析逻辑散落在 `tcpManager.ts`（正则）、`serial.ts`（手动解析）、`realtimeDataParser.ts`（策略模式）中。
2.  **硬编码**：前端 `index.vue` 中大量拼接字符串生成指令，难以维护。
3.  **命名歧义**：`deviceId` (声通ID)、`code` (机器鱼ID)、`microwaveIp` (实际存的是串口路径) 等命名严重误导开发。

### 重构计划

#### 第一步：数据库与命名重构 (Database & Naming)
**目标**：消除命名歧义，使字段名与其物理含义一致。
1.  **修改 Prisma Schema (`schema.prisma`)**：
    *   `deviceId` -> **`acousticId`** (声通通信ID，如 2)
    *   `code` -> **`fishCode`** (机器鱼业务编号，如 01)
    *   `microwaveIp` -> **`serialPortPath`** (串口路径，如 COM1)
    *   `microwavePort` -> **`serialBaudRate`** (波特率)
2.  **数据迁移**：生成并运行迁移脚本。
3.  **全栈同步**：更新后端 Service、前端 Store (`fishControl.ts`) 和 Vue 组件中的字段引用。

#### 第二步：构建统一协议层 (Protocol Layer)
**目标**：将通信协议与传输层（TCP/串口）解耦。
1.  **创建 `src/main/protocol` 模块**：
    *   **`CommandGenerator.ts`**：统一生成发送指令。
        *   `buildAcousticCmd(targetId, type, params)` -> `+++AT*SENDIM...`
        *   `buildIridiumCmd(targetId, type, params)` -> `PICSTART...`
    *   **`ProtocolParser.ts`**：统一解析接收数据。
        *   `parseAcoustic(string)` -> 返回 `{ type: 'ALARM' | 'STATUS', data: ... }`
        *   `parseIridium(string)` -> 返回 `{ type: 'SURF' | 'IMAGE_CHUNK', data: ... }`
        *   迁移并优化 `tcpManager.ts` 和 `serial.ts` 中的正则逻辑。

#### 第三步：后端通信重构 (Backend Communication)
**目标**：主进程作为协议处理的唯一真理来源。
1.  **重构 `src/main/ipc/serial.ts`**：
    *   移除内部解析逻辑，调用 `ProtocolParser.parseIridium`。
    *   处理图片分片逻辑封装到独立 Service。
2.  **重构 `src/main/ipc/tcp.ts` & `tcpManager.ts`**：
    *   调用 `ProtocolParser.parseAcoustic` 处理声通消息。
3.  **新增 `IPC` 接口**：
    *   `fish:sendCommand`：前端仅发送意图 `{ fishId, command: 'UP' }`，由后端生成指令并发送。

#### 第四步：前端 UI 清理 (Frontend Cleanup)
1.  **`index.vue`**：
    *   删除所有 `generateCmd` 字符串拼接代码。
    *   表单字段更新为 `acousticId` 等新名称。
2.  **移除冗余逻辑**：
    *   如果后端解析足够完善，可简化前端 `realtimeDataParser.ts`，直接消费后端解析后的结构化数据。

### 预期结果
*   **代码清晰**：所有协议格式定义只存在于 `src/main/protocol`。
*   **易扩展**：新增指令只需在 Generator 中添加一个 case。
*   **无歧义**：变量名直接反映业务含义。

请确认是否按照此计划执行？确认后我将分步开始实施。