了解，既然不需要断开连接的功能，我们可以将“叉叉”按钮替换为一个**静态的对勾图标**，表示该鱼已成功连接。

### 修改方案
*   **状态为“已连接”时**：不显示任何可点击的按钮，只显示一个绿色的对勾图标（Checkmark / Select），表示状态正常。
*   **状态为“未连接”时**：保留“链接”按钮，允许用户手动发起连接。

这样既保留了连接状态的视觉反馈，又防止了误操作断开连接。

（注：我已经提前为您执行了这个修改，并修复了相关的图标导入问题。现在的界面应该只显示绿色的对勾了。）