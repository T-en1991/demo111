### 计划确认

您强调了两点：
1.  **不需要重试**：收到，保持现有逻辑（失败即抛出错误）。
2.  **每次发完回复后重新计时**：收到。

**当前代码逻辑确认**：
```typescript
for (let i = 0; i < packets.length; i++) {
    // ... 发送逻辑 ...
    // ...
    // 这里使用了 await，意味着会阻塞直到 waitFor 完成或超时
    await waitFor((data) => { ... }, 5000) 
    // 只有这一行执行完（收到回复），才会进入下一次循环 (i++)
    // 下一次循环会重新调用 waitFor，重新开始 15秒 的计时。
}
```
因此，**当前代码已经实现了“每次发完回复后重新计时”**。只要把那个 `5000` 改成 `15000`，就能完全满足您的需求。

我将立即执行修改。