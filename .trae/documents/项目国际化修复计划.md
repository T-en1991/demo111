我将分两步来完成整个项目的国际化修复工作：

### 1. 更新语言配置文件 (`src/renderer/src/locales`)
我将在 `zh-CN.ts` 和 `en-US.ts` 中补充缺失的翻译键值，涵盖通用操作、机器鱼管理、顶部栏等模块。

**新增翻译项预览：**
- **`common`**: 添加 `play` (播放), `pause` (暂停), `progress` (进度), `volume` (音量), `language` (语言) 等。
- **`fish`**: 添加 `id` (鱼ID), `acousticId` (声通鱼端ID), `showOnMap` (地图显示), `initialLon` (初始经度), `initialLat` (初始纬度) 及其占位符。
- **`topbar`**: 添加 `connectionManager` (连接管理), `connect` (连接)。
- **`screen`**: 添加 `reconnecting` (重连中)。
- **`history`**: 补充 `alt` (高度) 映射。
- **`upload`**: 添加 `fileTypeHint` (文件类型提示)。

### 2. 替换硬编码中文 (`src/renderer/src`)
我将遍历以下文件，将硬编码的中文文本替换为对应的 `t('...')` 国际化函数调用：

- **`components/VideoPlayerJSMpeg.vue`**:
  - `进度` -> `t('common.progress')`
  - `音量` -> `t('common.volume')`
  - `播放/暂停` -> `t('common.play')` / `t('common.pause')`

- **`components/TopBar.vue`**:
  - `连接管理` -> `t('topbar.connectionManager')`
  - `连接` -> `t('common.connect')`
  - `中文` -> `t('common.language')`

- **`views/fish/index.vue`**:
  - 表单标签：`鱼ID`, `声通鱼端ID`, `地图显示`, `初始经度`, `初始纬度` -> 对应 `t('fish.*')`
  - 占位符：`请输入鱼ID`, `rtsp://...` -> 对应 `t('fish.*')`
  - 状态显示：`显示/隐藏` -> `t('common.show')` / `t('common.hide')` (复用现有 key)

- **`views/upload/index.vue`**:
  - 文件类型提示文本 -> `t('upload.fileTypeHint')`

执行此计划后，您的项目将支持完整的中英文切换，且不再有遗漏的硬编码中文字符串。