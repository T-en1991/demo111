修复了 TopBar 显示逻辑。

**原因**：
之前的代码逻辑是硬编码取列表中的**第一条鱼** (`list[0]`) 来显示，因此无论您切换选中哪条鱼，顶部按钮始终显示第一条。

**修复**：
修改了 `currentFishStatus` 的计算逻辑，现在它会根据 `fishControlStore.activeFishId`（当前选中的鱼 ID）去查找对应的鱼和状态。
这意味着：
1.  当您在下拉列表中点击切换鱼时，顶部按钮的名称和状态会同步更新。
2.  当您在地图上点击鱼图标切换时，顶部按钮也会同步更新。

现在界面应该能正确反映当前选中的鱼了。