已完成所有修改并验证。

### 修改总结
1.  **导航点精度与协议更新 (`FishControlStore.ts`)**
    *   **精度提升**：将经纬度编码从 4 位小数改为 6 位小数 (`* 1000000`)。
    *   **分包逻辑**：确认了每次发送 2 个点，并在最后一个包补齐（如果只剩一个点）。
    *   **确认机制**：发送完一个包（如 `P1`）后，现在会等待包含 `CMD-OK` 和序列号（如 `P1`）的回执，且支持回执中带有空格（`CMD-OK, P1`）。

2.  **前端输入与显示 (`views/fish/index.vue`)**
    *   **输入框精度**：将轨迹点编辑表格中的经纬度输入框精度从 4 位改为 6 位 (`:precision="6"`, `:step="0.000001"`)。
    *   **保存逻辑**：`round6` 函数确认已实现为保留 6 位小数，确保保存到数据库的数据精度正确。

### 下一步建议
请您在“机器鱼管理”页面重新编辑轨迹点，输入高精度坐标（6位小数），然后尝试在“屏幕监控”页面下发导航指令。程序应该会按照新的协议（P1, P2... PE）逐个发送并等待确认。