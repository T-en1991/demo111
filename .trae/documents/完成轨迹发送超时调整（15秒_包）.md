已完成修改。

### 修改内容
在 `src/renderer/src/store/fishControl.ts` 中，将 `sendTrajectory` 方法中的 `waitFor` 超时时间从 `5000` 改为了 `15000`（15秒）。

### 逻辑确认
由于 `waitFor` 是在 `for` 循环内部调用的，每次发送一个新的包（P1, P2...），都会重新进入 `waitFor`，**超时计时器会重新开始计算 15 秒**。
这意味着如果第一个包在 14秒收到回复，第二个包会有全新的 15秒等待时间，完全符合您的“每次发完回复后重新计时”的要求。