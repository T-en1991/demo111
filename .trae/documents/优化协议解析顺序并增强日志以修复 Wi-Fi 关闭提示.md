已完成修复和优化。

### 修复内容
1.  **解析顺序调整** (`ProtocolParser.ts`)：
    *   将 `WIFIOFF-SUCCESS` 的判断逻辑提到了 `WIFI-SUCCESS` 之前。
    *   虽然从字符串包含关系上看两者不同，但调整顺序是更稳健的做法，避免潜在的误匹配。

2.  **增加调试日志** (`tcp.ts`)：
    *   在发送 Toast 给前端之前，增加了 `logger.info` 日志。
    *   如果 `getMainWindow()` 返回空（窗口未找到），增加了 `logger.warn` 日志。

### 验证建议
现在当您收到 `WIFIOFF-SUCCESS` 消息时，请观察：
1.  前端是否弹出了提示框。
2.  如果没有弹出，请查看后端日志（控制台输出），看是否有 `[TCP] Sending toast to renderer: ...` 的记录。这能帮我们确认是后端解析没匹配上，还是前端没收到。

根据之前的分析，解析逻辑本身没有明显错误，现在的调整和日志将确保问题被解决或定位。