### 导航点发送逻辑重构计划

您提出了非常明确的导航点下发协议需求，我将分步骤为您实现。

#### 核心需求梳理
1.  **经纬度精度**：由 4 位小数改为 **6 位小数**（例如：`31.251134` -> `31251134`）。
2.  **分包协议**：
    *   **格式**：每次发送 2 个点（P1, P2... PE）。
    *   **内容**：`P<n>,<lat1>,<lon1>,<D>,<A>|<lat2>,<lon2>,<D>,<A>`。
    *   **补齐**：如果最后一个包只有一个点，复制该点补齐为两个点。
    *   **结束符**：最后一个包标记为 `PE`。
3.  **确认机制**：
    *   发送 `P1` 后，必须等待 `+++AT:51:RECVIM, ..., CMD-OK, P1` 回执。
    *   收到确认后才能发送 `P2`，直到 `PE` 发送完毕。

#### 实施步骤

1.  **修改 `FishControlStore.ts` 中的 `sendTrajectory` 方法**
    *   **精度调整**：将 `Math.round(p.lat * 10000)` 改为 `Math.round(p.lat * 1000000)`（6位）。
    *   **分包逻辑**：保持现有的“两点一组”逻辑，但需要确保最后一个点的补齐逻辑正确（当前代码似乎是 `packets.push([track[i], track[i]])`，这符合需求）。
    *   **序列号生成**：保持 `P1` 到 `PE` 的逻辑。
    *   **等待 ACK**：
        *   当前代码已有 `await waitFor((data) => data.includes(expect), 5000)`。
        *   需要确认 `expect` 的格式是否正确。您提供的例子是 `CMD-OK，P1`（注意可能有空格），我会调整匹配逻辑以更鲁棒（例如 `includes('CMD-OK') && includes(seq)`）。

2.  **验证 `round6` 函数 (`views/fish/index.vue`)**
    *   您提到 `views/fish/index.vue` 中的经纬度显示也需要从 4 位改为 6 位。我会检查 `round6` 函数的实现（目前看名字已经是 round6，但需确认逻辑）。

3.  **CommandGenerator 检查**
    *   检查 `src/main/protocol/CommandGenerator.ts` 是否有硬编码的逻辑。通常发送逻辑在 Store 层拼装好 payload 后，CommandGenerator 只是负责加 AT 头，这部分应该不用大改，但需确认。

### 具体的修改计划
*   **FishControlStore.ts**:
    *   修改 `fmt` 函数：经纬度乘数改为 `1000000`。
    *   优化 `waitFor` 的匹配条件，支持您提供的 `CMD-OK, P1` 格式（特别是处理逗号后的空格）。
*   **Views**:
    *   确保前端输入和显示都支持 6 位小数。

我将立即开始执行这些修改。