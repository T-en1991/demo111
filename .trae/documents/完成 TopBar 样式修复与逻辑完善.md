### 总结
1.  **修复了选中态样式**：通过添加 `!important` 和预留透明边框 (`border-left: 3px solid transparent`)，解决了选中项高亮背景和左侧蓝条不显示的问题。
2.  **修复了事件冒泡**：在 `toggleConnection` 中正确添加了 `event.stopPropagation()`，防止点击“连接/断开”按钮时意外触发“切换鱼”的操作。
3.  **UI 细节**：确认了图标颜色和间距。

现在：
1.  顶部菜单的 UI 已经美化（双行文本、圆角按钮）。
2.  下拉列表中点击某一行，可以正确切换当前选中的鱼（背景高亮变化，且 Screen 页面内容同步更新）。
3.  地图上的 Marker 点击也能联动切换。

请您再次体验，如果还有样式细节需要微调，请随时告诉我。