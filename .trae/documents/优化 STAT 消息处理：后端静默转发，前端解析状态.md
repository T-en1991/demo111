### 确认 STAT 格式

您确认了例子 `STAT,ID=01,125.25,...` 是正确的。这意味着数据中**确实包含** `ID=01` 这一项。

因此，我的解析计划保持不变：
`STAT` (0)
`ID=01` (1)
`yaw` (2)
`pitch` (3)
`roll` (4)
`lon` (5)
`lat` (6)
`depth` (7)
`alt` (8)
`battery` (9)
`signal` (10)
`time` (11)

之前的代码是直接取 index 1 作为 yaw，这是不对的（因为 index 1 现在是 ID）。我将把所有字段的索引 **+1**，以跳过 ID 字段。

我将立即修改前端解析逻辑和后端处理逻辑。